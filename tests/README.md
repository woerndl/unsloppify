# Test cases

Sixteen manual cases. Run them against the skill in a fresh session: ask the
agent to audit each Input with the unsloppify skill, then compare its findings
to Must flag and Must not flag. A case passes when every Must flag item is
found and no Must not flag item is reported. Run them before any release that
changes SKILL.md, the patterns, or the phrase list.
